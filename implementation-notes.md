# Implementation Notes — Agora Scout

## 2026-05-29 — Public commit-then-act verifier
- **Decisión:** Endpoint read-only `GET /api/verify/{id}` + botón "Verify on-chain" por rebalance. Verificación en 2 niveles: (L1) decision_hash guardado == calldata on-chain; (L2) recompute SHA-256 del payload canónico == decision_hash. Juntos = cadena `payload → hash → on-chain`.
- **Razón:** El pitch reclamaba "tamper-proof, verifiable on Arcscan" sin forma de verificarlo. Esto lo vuelve un feature provable = entrega el "audit layer" prometido en el grant de Circle. EV+ alto para jueces de un primitive de accountability.
- **Tradeoff:** Requirió persistir `decision_payload` canónico nuevo en tabla rebalances (antes solo guardaba el hash → no recomputable). Migración idempotente. Filas legacy (sin payload) verifican solo L1; nuevas obtienen L1+L2.
- **Seguridad:** Endpoint 100% read-only (cero writes/capital). `rebalance_id: int` en path → non-int = 422 (anti-injection). RPC solo lectura (`eth_getTransactionByHash`) vía cliente web3 key-free.
- **Cambios al plan:** VPS config.py y requirements.txt estaban DIVERGENTES del repo local (config sin ARC_PRIVATE_KEY/CHAIN_ID/ARCSCAN_TX_BASE; requirements sin web3==7.6.0). El `--build` reveló la divergencia (web3 faltaba → rompía anchoring+verifier). Fix: append 3 attrs a config.py VPS + scp requirements.txt correcto. VPS git queda divergente (chain.py untracked allí) — deploy por scp directo al working tree, NO git pull.
- **Verificado:** unit tests 4/4 (hash recompute + tamper); /api/verify/113 live → verified=true (L1, calldata==hash); 404 missing / 422 non-int OK; público vía funnel OK; frontend sirve VerifyButton sin errores transform.

## 2026-06-08 — Fix layout dashboard: safe-card colapsada (tira vertical)
- **Síntoma:** En `/dashboard` ancho, el `.safe-card` (veredicto + meter + confianza) colapsaba a una tira fina vertical en el centro; el donut se comía el resto → centro vacío.
- **Causa:** Edits locales sin commitear cambiaron `.status-row` y `.dec-grid` de CSS Grid → flexbox (`flex:1.45 1 0;min-width:0`). En flex, el safe-card se encogió a min-content pese a `min-width:0`; el donut con `flex:1` tomó el espacio. La versión Grid (committeada) funcionaba.
- **Decisión:** Revertir ambos a Grid pero con `grid-template-columns:minmax(0,1.45fr) minmax(0,1fr)` (status-row) y `minmax(0,1.4fr) minmax(0,1fr)` (dec-grid). `minmax(0,…)` llena el track Y evita blowout por texto largo — más robusto que el Grid original sin minmax. Tradeoff: Grid sobre flex porque los tracks de Grid siempre honran el sizing; flex colapsa a min-content en este árbol.
- **Conservado:** spans nowrap en App.jsx (arc-pill, market-flag) y bloque guard `flex-direction:row !important` — inofensivos, atacan el bug previo de stacking vertical.
- **Deploy:** Producción NO es build estático — el contenedor `frontend` corre `npm install && npm run dev` (Vite dev, volumen `./frontend:/app`, puerto 3003). El sitio live está en VPS `<vps-ip>` (tailscale `agora-scout.tail127286.ts.net`). `npm run build` local es irrelevante (y falla por node_modules local sin react-router-dom — no afecta prod). Fix entregado por `scp` de `dashboard.css` + `App.jsx` al working tree del VPS → Vite HMR recarga vía volumen. NO se usó deploy.sh (hace `build --no-cache backend`, innecesario para CSS).
- **Verificado:** scp OK (uploaded). Falta confirmación visual del usuario tras hard-refresh.

## 2026-06-08 (cont.) — CAUSA RAÍZ REAL: colisión de clase `side` (no era el grid)
- **Lo de grid→flex fue desvío.** El revert a Grid no arreglaba el visual. Diagnóstico vía DevTools (computed styles del usuario): el `.market-flag` medía **1214px de alto** (`position:sticky`, `height:calc(100vh-64px)`), empujando verdict-big/safe-meter/conf a `top:1352px` (fuera de pantalla). Por eso el panel se veía como tira vacía.
- **Causa:** la clase `side` se usa para DOS cosas distintas: (1) `<aside className="side">` = sidebar, con regla `.agora-dash .side{position:sticky;top:64px;height:calc(100vh-64px)}`; (2) `rclass('SIDEWAYS')` devuelve `'side'`, aplicado al market-flag (`market-flag side`) y a los `.mtag side` del log. El selector descendente `.agora-dash .side` matchea CUALQUIER hijo con clase `side` → el flag heredaba la altura del sidebar. Solo rompía en régimen SIDEWAYS (bear/bull no colisionan). Los tags del log no explotaban porque `.mtag` tiene `max-height:25px` que capa la altura; el market-flag no tenía cap.
- **Fix:** calificar la regla del sidebar a `aside.side` (base + media query). Cambio mínimo, sin tocar JS. El span del flag ya no matchea.
- **Descartado:** NO era extensión de navegador (reprodujo en incógnito). NO era caché (DevTools confirmó computed real). El `#adopted-style-sheets` fue otro desvío.
- **Deploy:** scp dashboard.css al VPS + `docker restart agora-scout-frontend` (HMR sobre volumen no recargaba fiable). Verificado: served CSS = `aside.side{...}`.
- **Footgun pendiente:** clase genérica `side` como modificador de régimen es peligrosa. Mejor a futuro: renombrar `rclass` 'side'→'sideways' y los selectores `.mtag.side`/`.market-flag.side`. Por ahora el `aside.side` corta el sangrado.

### Deuda técnica detectada (no bloqueante)
- Edits del dashboard sin commitear en working tree local — commitear el fix para no re-perderlo. node_modules local de frontend incompleto (sin react-router-dom) → `npm run build` local no corre; instalar si se quiere build local.
- ~~WebSocket en vivo roto: `usePortfolioData.js` conectaba a `ws://localhost:8000/ws/feed`~~ **RESUELTO 2026-06-08:** el fallback `WS_URL` era `ws://localhost:8000/ws/feed` hardcodeado → desde navegador remoto resolvía a la máquina del cliente → fallaba y reintentaba cada 5s (spam, sin feed en vivo; datos cargaban por REST poll). Fix: derivar URL same-origin (`${protocol==='https'?'wss':'ws'}://${location.host}/ws/feed`) → va por el proxy `/ws` (ws:true) de vite.config al backend (que sí tiene `@app.websocket("/ws/feed")`, main.py:302). Riesgo nulo: path de fallo intacto (try/catch + retry 5s). Deploy scp + restart vite. Commiteado.
- VPS `/root/agora-scout` git divergente del remote (HEAD anterior al commit de anchoring; chain.py untracked). Deploy actual = scp al working tree. Reconciliar VPS git con GitHub en sesión futura para evitar drift.
- VPS config.py / requirements.txt estaban desincronizados del repo — revisar que VPS quede igual a GitHub.

## 2026-06-10 — Real native-USDC transfers on Arc (grant lever #1) + nonce fix
- **Decisión:** Nuevo `backend/arc_transfer.py` (`ArcTransfer`) mueve USDC NATIVO real en Arc entre treasury (`0x2D10…849E`, bucket safe = cash+T-bills) y un vault generado (`0x6fb246…1842`, bucket at-risk = DeFi yield). En Arc USDC es el token nativo de gas/valor (18 dec) → "mover capital" = transfer nativo `value>0`, NO ERC-20. Gated por `EXECUTION_MODE=live` + `ARC_VAULT_PRIVATE_KEY`. Default `simulate` (cero cambio de comportamiento al deployar). Integrado en `executor.execute_rebalance` DESPUÉS del anchor → commit-then-act real punta a punta: (1) anchor sha256(decisión) zero-value, (2) transfer USDC real por esa decisión.
- **Razón:** El grant de Circle exige "Arc core to flow of value/settlement". Antes el rebalance era 100% simulado (solo % en SQLite + anchor). Ahora el capital se mueve de verdad on-chain por decisión del agente = requisito #1 cumplido y verificable en Arcscan.
- **plan_move (pura, testeable):** vault debe quedar en ~yield_pct del combinado treasury+vault; mueve el delta, dust<0.001 USDC se salta, reserva 3× gas (nunca drena). `test_arc_transfer.py` (5 tests) cubre ambas direcciones + clamp + dust. Verificado en contenedor descartable.
- **BUG REAL atrapado por verificación (no asumido):** anchor (treasury, sin esperar receipt) + transfer (treasury) chocaban nonce → `nonce too low: next 407, tx 406`. Habría roto TAMBIÉN el loop live real, no solo el demo. **Fix:** nonce `"pending"` (incluye mempool) en `chain.py._anchor_sync` y `arc_transfer._send_sync` → sends secuenciales de la misma wallet quedan en orden. Re-deploy + rebalance limpio confirmó fix.
- **Otros cambios:** `/api/health` (el proxy Vite solo rutea `/api`→backend, `/health` pelado daba HTML del SPA) con `execution_mode/live_transfers/anchoring_enabled`; CORS de `*` → lista (`ALLOWED_ORIGINS` env, default tailscale host + localhost); columnas DB `transfer_tx_hash/amount/direction` (migración idempotente, mismo patrón); `get_rebalances` expone `transfer_arcscan_url`.
- **Deploy:** backend es `build: .` (código horneado, solo `./data` volumen). scp archivos → `docker compose build backend && up -d`. Backup previo: `/root/backups/agora-scout-20260610/` (DB 2.7MB + project.tar.gz). DB en `/root/agora-scout/data/agora_scout.db`.
- **VERIFICADO por OUTPUT real:** (1) transfer aislado tx `0x0a0850f6…` movió 3.99 USDC treasury→vault (treasury 19.94→15.95, vault 0→3.99). (2) rebalance full-path **row #463**: anchor `0x2d5748…` + transfer real `0xee7045…` 2.39 USDC treasury→vault. (3) `/api/verify/463` → verified=True (payload→hash→on-chain ambos niveles). (4) `/api/health` → `execution_mode=live, live_transfers=true`. (5) DB tracción preservada (2268 decisiones / 463 rebalances, sube). Row 462 quedó con transfer=None (víctima del bug nonce antes del fix — inofensivo, anchor OK).
- **Pendiente:** frontend debe mostrar el transfer_arcscan_url (hoy solo muestra anchor); botón Run Agent; build de producción (frontend corre `npm run dev`); narrativa grant (quitar claim Gateway falso, reposicionar treasury, revenue).
- **Footgun:** loop live mueve USDC real en cada cambio de régimen (testnet, ~16 USDC treasury, moves sub-USDC, gas ~0.0004/tx — miles de tx posibles). Si el régimen hace ping-pong SIDEWAYS↔BEAR quema gas; aceptable en testnet, es la actividad/tracción buscada.

## 2026-06-11 — Multi-tenant self-serve accounts (unlock para grant M1 + Lepton)
- **Contexto/estrategia:** NO ganamos Agora (winners 14 jun). Causa #1 = tracción (41 visitantes, 0 usuarios); jueces premiaron "early traction" (1er lugar Mimir lo cita), y lo mejor que construimos (real transfers) llegó DESPUÉS del deadline 25 may → calificaron la versión débil. Lección par-a-par: el build nunca fue el cuello, distribución sí. Grant (review activo, eval absoluta) + Lepton (15-29 jun, juzga progreso post-15, penaliza "building in the blind") + feedback de jueces apuntan TODOS a lo mismo: conseguir 3-5 usuarios reales. Por eso el siguiente build = convertir espectadores en usuarios, no más features.
- **Decisión:** `backend/accounts.py` — multi-tenant self-serve en testnet. Cada usuario = par de wallets (cash + yield) generadas server-side. El usuario fundea su `deposit_address` (cash) vía faucet.circle.com; el agente rebalancea USDC NATIVO real entre cash↔yield por usuario con el MISMO régimen global (reusa `ArcTransfer.plan_move` + patrón send pending-nonce). Retiro a cualquier dirección. ADITIVO: no toca el loop showcase single-treasury.
- **Por qué testnet + tu recomendación:** cero riesgo monetario, custodia testnet = sin responsabilidad legal seria, suficiente para grant M1 + demo Lepton. Mainnet exigiría Circle Wallets custody/KMS (roadmap).
- **Endpoints:** `POST /api/account` (crea, devuelve deposit_address + secret), `GET /api/account/{id}` (estado on-chain live + rebalances; NO filtra keys), `POST /api/account/{id}/withdraw` (gated por secret), `GET /api/accounts/stats`. Loop: `accounts.rebalance_all_funded(yield_pct)` tras el showcase, guarded try/except.
- **Seguridad:** secret por-cuenta (bearer, uuid) gatea withdraw — sin esto, account_id secuencial = cualquiera drena. Keys en DB PLAINTEXT (testnet pilot only; flag mainnet→custody). Endpoint GET sanitiza (solo addresses+balances, nunca keys — verificado `keys_leaked: False`).
- **DB:** tablas `accounts` (id,label,secret,cash/yield addr+key) + `account_rebalances`; `total_accounts`/`total_account_rebalances` en /api/traction.
- **VERIFICADO end-to-end on-chain (one-shot DB prod):** crear id=1 → fundear 2 USDC treasury→deposit (tx real) → rebalance_account(20) movió cash→yield 0.4 USDC real → estado cash 1.6/yield 0.4/yld%=20.0 exacto → withdraw 2 transfers a dirección externa con secret → total ~0 (dust=reserva gas) → bad-secret = invalid_secret. HTTP: POST/GET/stats OK, keys NO filtradas. Cuentas de test BORRADAS post-verificación (traction honesta = 0 usuarios reales hasta pilotos reales).
- **PENDIENTE:** frontend onboarding ("Manage your own USDC": crear cuenta, mostrar deposit address + faucet, posición live + rebalances + verify, withdraw). Es lo que falta para que un usuario real lo use self-serve. Luego: conseguir los 3-5 pilotos reales (GTM, no código).
- **Deuda:** _send_sync duplicado entre arc_transfer.py y accounts.py (no refactoricé para no tocar el path live que ya jala). Encriptar keys (Fernet) antes de cualquier mainnet.

## 2026-06-18 — Pivote a nanopagos x402 para Lepton (RFB 02) + análisis vault ARC
- **Contexto:** NO ganamos Agora. Siguiente = Lepton (15-29 jun), tema NANOPAGOS (agentes pagando sub-centavo USDC vía Gateway/x402 en Arc). Rúbrica: 30% traction ("payments flowing in test USDC, volume"), 20% Circle-tools (Gateway/x402/Nanopayments), 30% agentic, 20% innovation. Agora Scout (treasury) NO encaja nativo → doblar a **RFB 02 "selling agent services via nanopayments, pay-per-call"**: el agente cobra su trabajo como nanopago x402. Esencia (regime agent + commit-then-act verifier) intacta.
- **Vault re-análisis (free-developments):** confirmó munición — YieldPulse (proyecto propio que GANÓ Lablab) tiene x402 + Circle DCW SDK funcionando; ref oficial circlefin/arc-nanopayments usa el SDK NUEVO `@circle-fin/x402-batching` (verify+settle real en Arc, Gateway batching). Conecté los dots del vault (Agora-Scout apuntaba a 0 notas del cluster ARC → fix bidireccional).
- **Decisión arquitectura:** `@circle-fin/x402-batching` es **JS-only** (no hay SDK Python). Python DCW SDK existe (`circle-developer-controlled-wallets` v9.6.0) pero mapea al modelo custodial demo-grade. Elegí **sidecar Node** (reusa el SDK oficial, verify+settle real) frente a FastAPI: Python sigue siendo el cerebro, sidecar = riel de pago. No necesita Circle API key (self-custodiado: keys + USDC faucet + Gateway SDK).
- **Construido este turno:**
  - `nanopay/` (sidecar): `server.mjs` (withGateway lifted de ref lib/x402.ts → `POST /paid/rebalance` gated $0.01 → proxy a FastAPI `/api/internal/rebalance` con X-Internal-Secret, pasa el pago settled), `pay-agent.mjs` (GatewayClient buyer → deposit → pay() loop), package.json (`@circle-fin/x402-batching ^2.0.4`), Dockerfile, .env.example. Arc constants: network eip155:5042002, USDC 0x3600…0000, Gateway Wallet 0x0077777d7EBA4688BDeF3E311b846F25870A19B9, RPC rpc.testnet.arc.network.
  - Backend: `INTERNAL_SECRET` config; tabla `nanopayments` + save/get/count; `POST /api/internal/rebalance` (gated por secret, sin rate-limit, registra el nanopago + corre ciclo); `/api/nanopayments` + `/api/nanopayments/stats`; `total_nanopayments` + `nanopayment_volume_usdc` en /api/traction.
- **Wallets riel (testnet):** SELLER (recibe fees) 0xdCFB59…8b77, BUYER (paga) 0x748072…EE65. Keys en VPS `/root/agora-scout/nanopay/.env.wallets`. PEND usuario: fondear BUYER en faucet.circle.com.
- **VERIFICADO offline:** backend deployado, `/api/internal/rebalance`→403 sin secret, `/api/nanopayments/stats`→0/$0 (baseline honesto). Settlement real NO testeable hasta fondear BUYER.
- **PENDIENTE:** (1) usuario fondea BUYER; (2) deploy sidecar al VPS (3er container Node) + INTERNAL_SECRET compartido; (3) test full loop pay-agent→settle→internal rebalance→registrado; (4) frontend muestra "nanopayments settled / volume"; (5) demo del nanopago en Arcscan → submit forms.gle/SMqLaw2pMGDe58LFA (submits ilimitados). Video: grabar DESPUÉS del nanopago funcionando.
- **Caveats sidecar (del builder):** explorer URL Arc corregido a testnet.arcscan.app; gateway.pay() return fields (data/transaction) son asunción a verificar al instalar; X-Internal-Secret ahora SÍ enforced en main.py.

## 2026-06-18 (cont.) — x402-batching BLOQUEADO por SDK; Path A (nanopago directo) SHIPPED
- **x402-batching murió por regresión upstream:** sidecar Node desplegado al VPS (container `agora-nanopay`, red compose, /health OK). El pay-agent: funding wallet efímera ✓, depósito Gateway ✓ (tx real `0x0534…`), balance Gateway 1 USDC ✓, pero `facilitator.verify()` → `authorization_validity_too_short` SIEMPRE. DIAGNÓSTICO con data (logué el payload): cliente firma `validBefore=now+maxTimeoutSeconds`, facilitator exige `validBefore−now>=maxTimeoutSeconds`, los segundos entre firma y verify lo dejan 1-2s corto. El facilitator valida contra el `maxTimeoutSeconds` EMBEBIDO en el payload firmado (no el de mi requirements→no puedo relajarlo sin invalidar la firma EIP-3009). Intentos fallidos: bajar maxTimeoutSeconds (600), relajar verifyReq (−300), pinear 2.0.4/2.6.0, npm `overrides @x402/core 2.6.0`. Root cause: `^2.6.0` instaló `@x402/core 2.15.0` (regresión); el ref ganó cuando 2.6.x era latest, sin lockfile→irreproducible. NO vale seguir cavando (decisión usuario: "anda con A"). Sidecar queda en repo `nanopay/` como integración Gateway/x402 documentada (código correcto, bloqueado por versión upstream) — cuenta como uso de Circle tooling para jueces.
- **Path A SHIPPED (nanopago directo, todo Python):** `accounts.pay_nanofee(from_key,to_addr,amount_usdc)` = transfer USDC NATIVO sub-centavo real en Arc (reusa `_send_sync` probado). Config `NANOPAY_BUYER_PRIVATE_KEY/SELLER_ADDRESS/FEE_USDC=0.001` (wallets del riel: BUYER 0x7480 fondeado 20 USDC faucet, SELLER 0xdCFB). Endpoint `POST /api/pay-rebalance` (rate-limited): buyer paga seller 0.001 USDC on-chain → registra nanopago → corre ciclo agente. Frontend: botón "Pay & run · ◎0.001" en topbar dashboard (toast con Arcscan del pago) + footer "N nanopayments · X USDC".
- **VERIFICADO por OUTPUT real:** nanopago aislado tx `0xe88683…` (buyer→seller 0.001 USDC) registrado. Loop server-side 15× → 17 nanopagos / $0.017 volumen. HTTP `/api/pay-rebalance` → tx `0x8efa…` + régimen BEAR. /api/traction `total_nanopayments:17, nanopayment_volume_usdc:0.017`. Frontend build 94 módulos OK, dashboard 200, métrica via proxy. Sin sidecar Node en path A (todo FastAPI/Python).
- **Para Lepton:** "payments flowing in test USDC + volume" (30% traction) = CUMPLIDO con txs reales on-chain. Circle-tools (20%): USDC+Arc nativo real; x402/Gateway documentado pero bloqueado. PENDIENTE: generar más volumen (loop on-demand o usuarios reales), grabar demo del nanopago fluyendo en Arcscan, submit `forms.gle/SMqLaw2pMGDe58LFA`.

## 2026-06-17 — Casper Agentic Buildathon port map (CASPER-PORT.md)
- **Decisión:** documentar (no construir aún) el port Agora→Casper Buildathon ($150k, deadline 2026-07-01, x402 sponsored). Mapa en `CASPER-PORT.md`.
- **Hallazgo clave:** la capa chain ya está aislada → ~83% del backend reusa verbatim (agent/data/executor/database/main/accounts + `plan_move` + `hash_decision` + frontend). Adapter Casper = reescribir solo chain.py + arc_transfer.py (~250-350 líneas) contra `pycspr` (verificado existe: transfer + install contract), MISMA interfaz async. Anchor = deploy named-arg (no calldata EVM); capital = CSPR nativo (Arc USDC nativo ≠ CSPR, declarar honesto). x402 = cada decisión micropago-gated → pega Agentic AI + DeFi&Payments.
- **Razón / tradeoff:** GO-CONDITIONAL como SEGUNDO tiro post-Lepton, NO antes. Lepton (cierra 29-jun) = lever del Circle grant M1 = prioridad. Casper solo si Lepton wrapea con ≥3 días buffer; no canibalizar. Deuda _send_sync duplicado tocaría también al adapter — refactor a interfaz común si se hace el port.
- **Verificado:** pycspr + x402 facilitator live/patrocinado vía websearch. NO se escribió código de adapter (premature antes de decidir + antes de Lepton).
